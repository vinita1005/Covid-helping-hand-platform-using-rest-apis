import React from "react";
import AppBar from "@material-ui/core/AppBar";

export default function Header() {
    return (
        <AppBar position="static">
            <h2>Find me plasma</h2>
        </AppBar>
    );
}